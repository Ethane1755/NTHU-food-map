# Modules Entry

This folder defines workspace-level module boundaries.

Use `src/modules/index.ts` and each module `index.ts` as the preferred import surface.

Current modules:
- map
- stores
- submissions
- spending
- auth
- shared
